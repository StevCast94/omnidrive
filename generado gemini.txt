Como modelo de lenguaje de IA, no puedo generar ni exportar archivos PNG físicos directamente. Sin embargo, para un flujo de trabajo moderno en React, los SVGs son superiores, ya que son infinitamente escalables, pesan menos y pueden manipularse con Tailwind. He diseñado las tres variantes de tu logo como componentes SVG precisos que puedes usar directamente en tu código, siguiendo exactamente tu concepto de la cinta de Möbius geométrica y el degradado cian-índigo.  A continuación, tienes el rediseño completo de OmniDrive basado en tu sistema de diseño.1. Logo (Variantes SVG)Crea un archivo src/components/ui/Logo.tsx para almacenar las variantes. El monograma utiliza trazos geométricos superpuestos para simular la cinta de Möbius.  TypeScript// src/components/ui/Logo.tsx
import React from 'react';

interface LogoProps extends React.SVGProps<SVGSVGElement> {
  variant?: 'icon' | 'full' | 'horizontal';
}

export const Logo: React.FC<LogoProps> = ({ variant = 'full', className = '', ...props }) => {
  const Icon = () => (
    <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg" className="w-full h-full">
      <defs>
        <linearGradient id="omni-grad" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" stopColor="#06b6d4" />
          <stop offset="100%" stopColor="#6366f1" />
        </linearGradient>
      </defs>
      <path d="M30 20 L70 20 A 20 20 0 0 1 90 40 L90 60 A 20 20 0 0 1 70 80 L30 80 A 20 20 0 0 1 10 60 L10 40 A 20 20 0 0 1 30 20 Z" stroke="url(#omni-grad)" strokeWidth="8" />
      <path d="M50 20 L50 80" stroke="url(#omni-grad)" strokeWidth="8" />
      <path d="M10 50 L90 50" stroke="#0f172a" strokeWidth="12" />
      <path d="M30 20 L70 80" stroke="url(#omni-grad)" strokeWidth="6" opacity="0.8" />
    </svg>
  );

  if (variant === 'icon') {
    return (
      <div className={`w-10 h-10 ${className}`} {...props}>
        <Icon />
      </div>
    );
  }

  if (variant === 'horizontal') {
    return (
      <div className={`flex items-center gap-3 ${className}`} {...props}>
        <div className="w-10 h-10"><Icon /></div>
        <span className="text-xl font-bold tracking-tight text-[var(--color-text-primary)]" style={{ fontFamily: 'var(--font-sans)' }}>
          Omni<span className="text-[var(--color-primary)]">Drive</span>
        </span>
      </div>
    );
  }

  // Full variant (vertical)
  return (
    <div className={`flex flex-col items-center gap-2 ${className}`} {...props}>
      <div className="w-16 h-16"><Icon /></div>
      <span className="text-2xl font-bold tracking-tight text-[var(--color-text-primary)]" style={{ fontFamily: 'var(--font-sans)' }}>
        Omni<span className="text-[var(--color-primary)]">Drive</span>
      </span>
    </div>
  );
};
2. Sistema de Diseño (CSS Global)Reemplaza el contenido de tu archivo CSS principal (ej. src/index.css o src/styles/tokens.css).CSS/* src/styles/tokens.css */
@import "tailwindcss";

@layer base {
  :root {
    --color-primary: #06b6d4;
    --color-primary-dark: #0891b2;
    --color-secondary: #6366f1;
    --color-accent: #22d3ee;
    --color-surface: #0f172a;
    --color-surface-raised: #1e293b;
    --color-surface-overlay: rgba(15, 23, 42, 0.85);
    --color-border: rgba(148, 163, 184, 0.1);
    --color-text-primary: #f8fafc;
    --color-text-secondary: #94a3b8;
    --color-text-muted: #64748b;
    
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 16px;
    --radius-xl: 24px;
    
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.3);
    --shadow-md: 0 4px 12px rgba(0,0,0,0.4);
    --shadow-lg: 0 8px 32px rgba(0,0,0,0.5);
    --shadow-glow: 0 0 20px rgba(6, 182, 212, 0.15);
    
    --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
    
    --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
    --transition-normal: 250ms cubic-bezier(0.4, 0, 0.2, 1);
    --transition-slow: 400ms cubic-bezier(0.4, 0, 0.2, 1);
  }

  body {
    background-color: var(--color-surface);
    color: var(--color-text-primary);
    font-family: var(--font-sans);
    -webkit-font-smoothing: antialiased;
  }
}

/* Utilidades personalizadas para Tailwind v4 */
@theme {
  --animate-fade-in: fadeIn 0.4s ease-out forwards;
  --animate-slide-up: slideUp 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  --animate-slide-down: slideDown 0.4s ease-out forwards;
  --animate-shimmer: shimmer 2s infinite linear;
  --animate-glow-pulse: glowPulse 2s infinite alternate;
  --animate-float: float 6s ease-in-out infinite;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes slideDown {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

@keyframes glowPulse {
  0% { box-shadow: 0 0 10px rgba(6, 182, 212, 0.2); }
  100% { box-shadow: 0 0 25px rgba(6, 182, 212, 0.6); }
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

/* Clases utilitarias globales */
.glass-panel {
  background: var(--color-surface-overlay);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--color-border);
}

.stagger-1 { animation-delay: 100ms; }
.stagger-2 { animation-delay: 200ms; }
.stagger-3 { animation-delay: 300ms; }
3. Componentes Base3.1. Button.tsxTypeScript// src/components/ui/Button.tsx
import React from 'react';
import { Loader2 } from 'lucide-react';

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  isLoading?: boolean;
}

export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className = '', variant = 'primary', size = 'md', isLoading = false, children, disabled, ...props }, ref) => {
    
    const baseStyles = "inline-flex items-center justify-center font-medium transition-all duration-[var(--transition-fast)] active:scale-[0.98] disabled:opacity-50 disabled:pointer-events-none rounded-[var(--radius-sm)]";
    
    const variants = {
      primary: "bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)] text-white hover:shadow-[var(--shadow-glow)] hover:brightness-110 border border-transparent",
      secondary: "bg-[var(--color-surface-raised)] text-[var(--color-text-primary)] hover:bg-[var(--color-surface-overlay)] border border-[var(--color-border)]",
      outline: "bg-transparent text-[var(--color-primary)] border border-[var(--color-primary)] hover:bg-[var(--color-primary)] hover:bg-opacity-10",
      ghost: "bg-transparent text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)] hover:bg-[var(--color-surface-raised)]"
    };

    const sizes = {
      sm: "h-9 px-4 text-sm",
      md: "h-11 px-6 text-base",
      lg: "h-14 px-8 text-lg rounded-[var(--radius-md)]"
    };

    return (
      <button
        ref={ref}
        disabled={disabled || isLoading}
        className={`${baseStyles} ${variants[variant]} ${sizes[size]} ${className}`}
        {...props}
      >
        {isLoading && <Loader2 className="w-4 h-4 mr-2 animate-spin" />}
        {children}
      </button>
    );
  }
);
Button.displayName = 'Button';
3.2. Input.tsxTypeScript// src/components/ui/Input.tsx
import React, { useState } from 'react';

interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label: string;
  error?: string;
}

export const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className = '', label, error, required, ...props }, ref) => {
    const [isFocused, setIsFocused] = useState(false);
    const hasValue = props.value !== undefined && props.value !== '';

    return (
      <div className={`relative w-full ${className}`}>
        <div className={`
          relative border rounded-[var(--radius-sm)] bg-[var(--color-surface-raised)] transition-all duration-[var(--transition-normal)]
          ${error ? 'border-red-500' : isFocused ? 'border-[var(--color-primary)] shadow-[var(--shadow-glow)]' : 'border-[var(--color-border)]'}
        `}>
          <label className={`
            absolute left-4 transition-all duration-[var(--transition-normal)] pointer-events-none
            ${isFocused || hasValue ? 'text-xs top-2 text-[var(--color-primary)]' : 'text-sm top-3.5 text-[var(--color-text-muted)]'}
          `}>
            {label} {required && '*'}
          </label>
          <input
            ref={ref}
            className="w-full bg-transparent px-4 pt-6 pb-2 text-[var(--color-text-primary)] outline-none"
            onFocus={(e) => { setIsFocused(true); props.onFocus?.(e); }}
            onBlur={(e) => { setIsFocused(false); props.onBlur?.(e); }}
            {...props}
          />
        </div>
        {error && <p className="mt-1 text-xs text-red-500 animate-fade-in">{error}</p>}
      </div>
    );
  }
);
Input.displayName = 'Input';
3.3. Card.tsxTypeScript// src/components/ui/Card.tsx
import React from 'react';

interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'raised' | 'glass';
}

export const Card: React.FC<CardProps> = ({ variant = 'raised', className = '', children, ...props }) => {
  const baseStyles = "rounded-[var(--radius-md)] transition-all duration-[var(--transition-normal)]";
  
  const variants = {
    raised: "bg-[var(--color-surface-raised)] border border-[var(--color-border)] hover:shadow-[var(--shadow-md)] hover:-translate-y-1",
    glass: "glass-panel"
  };

  return (
    <div className={`${baseStyles} ${variants[variant]} ${className}`} {...props}>
      {children}
    </div>
  );
};
3.4. Badge.tsxTypeScript// src/components/ui/Badge.tsx
import React from 'react';

interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'cyan' | 'indigo' | 'green' | 'amber' | 'red';
}

export const Badge: React.FC<BadgeProps> = ({ variant = 'cyan', className = '', children, ...props }) => {
  const variants = {
    cyan: "bg-cyan-500/10 text-cyan-400 border-cyan-500/20 shadow-[0_0_10px_rgba(6,182,212,0.1)]",
    indigo: "bg-indigo-500/10 text-indigo-400 border-indigo-500/20 shadow-[0_0_10px_rgba(99,102,241,0.1)]",
    green: "bg-emerald-500/10 text-emerald-400 border-emerald-500/20 shadow-[0_0_10px_rgba(16,185,129,0.1)]",
    amber: "bg-amber-500/10 text-amber-400 border-amber-500/20",
    red: "bg-rose-500/10 text-rose-400 border-rose-500/20",
  };

  return (
    <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border ${variants[variant]} ${className}`} {...props}>
      {children}
    </span>
  );
};
4. Vistas y Estructura4.1. Navbar.tsxTypeScript// src/components/Navbar.tsx
import React, { useState, useEffect } from 'react';
import { Menu, X, User } from 'lucide-react';
import { Logo } from './ui/Logo';
import { Button } from './ui/Button';

export const Navbar = () => {
  const [scrolled, setScrolled] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [isLoggedIn] = useState(false); // Simular estado de auth

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <nav className={`fixed top-0 w-full z-50 transition-all duration-[var(--transition-slow)] ${
      scrolled ? 'glass-panel py-3 shadow-lg border-b-[var(--color-border)]' : 'bg-transparent py-5 border-b-transparent'
    }`}>
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center">
          
          {/* Logo animado */}
          <div className="flex-shrink-0 animate-slide-down">
            <a href="/">
              <Logo variant="horizontal" className={scrolled ? 'scale-90 transition-transform' : 'transition-transform'} />
            </a>
          </div>

          {/* Enlaces Desktop */}
          <div className="hidden md:flex space-x-8 animate-fade-in stagger-1">
            {['Inicio', 'Vehículos', 'Cómo funciona'].map((item) => (
              <a key={item} href="#" className="text-[var(--color-text-secondary)] hover:text-white font-medium relative group transition-colors">
                {item}
                <span className="absolute -bottom-1 left-1/2 w-0 h-0.5 bg-[var(--color-primary)] group-hover:w-full group-hover:left-0 transition-all duration-300"></span>
              </a>
            ))}
          </div>

          {/* Auth Desktop */}
          <div className="hidden md:flex items-center space-x-4 animate-fade-in stagger-2">
            {isLoggedIn ? (
              <div className="w-10 h-10 rounded-full bg-[var(--color-surface-raised)] border border-[var(--color-primary)] flex items-center justify-center cursor-pointer hover:shadow-[var(--shadow-glow)] transition-all">
                <User className="text-[var(--color-primary)] w-5 h-5" />
              </div>
            ) : (
              <>
                <Button variant="ghost">Ingresar</Button>
                <Button variant="primary">Registrarse</Button>
              </>
            )}
          </div>

          {/* Mobile menu button */}
          <div className="md:hidden flex items-center">
            <button onClick={() => setMobileMenuOpen(!mobileMenuOpen)} className="text-[var(--color-text-secondary)] hover:text-white">
              {mobileMenuOpen ? <X size={28} /> : <Menu size={28} />}
            </button>
          </div>
        </div>
      </div>

      {/* Mobile Menu Overlay */}
      {mobileMenuOpen && (
        <div className="md:hidden absolute top-full left-0 w-full glass-panel border-t border-[var(--color-border)] animate-slide-down">
          <div className="px-4 pt-2 pb-6 space-y-4">
            {['Inicio', 'Vehículos', 'Cómo funciona'].map((item) => (
              <a key={item} href="#" className="block text-base font-medium text-[var(--color-text-secondary)] hover:text-white hover:pl-2 transition-all">
                {item}
              </a>
            ))}
            <div className="pt-4 border-t border-[var(--color-border)] flex flex-col gap-3">
              <Button variant="outline" className="w-full">Ingresar</Button>
              <Button variant="primary" className="w-full">Registrarse</Button>
            </div>
          </div>
        </div>
      )}
    </nav>
  );
};
4.2. Home.tsx (Hero Section)TypeScript// src/pages/Home.tsx
import React from 'react';
import { Button } from '../components/ui/Button';
import { Badge } from '../components/ui/Badge';
import { Navbar } from '../components/Navbar';

export const Home = () => {
  return (
    <div className="min-h-screen bg-[var(--color-surface)] overflow-hidden">
      <Navbar />
      
      <main className="relative pt-32 pb-20 lg:pt-48 lg:pb-32 flex items-center justify-center min-h-screen">
        {/* Background Effects */}
        <div className="absolute inset-0 z-0">
          <div className="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"></div>
          <div className="absolute left-0 right-0 top-0 -z-10 m-auto h-[310px] w-[310px] rounded-full bg-[var(--color-primary)] opacity-20 blur-[100px] animate-float"></div>
          <div className="absolute right-20 bottom-0 -z-10 h-[250px] w-[250px] rounded-full bg-[var(--color-secondary)] opacity-20 blur-[100px] animate-float" style={{ animationDelay: '2s' }}></div>
        </div>

        <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center animate-slide-up">
          <Badge variant="cyan" className="mb-6 px-4 py-1 text-sm animate-fade-in">
            El futuro de la movilidad compartida
          </Badge>
          
          <h1 className="text-5xl md:text-7xl font-extrabold tracking-tight text-white mb-6">
            Conduce cualquier auto,<br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)]">
              en cualquier lugar.
            </span>
          </h1>
          
          <p className="mt-4 max-w-2xl mx-auto text-xl text-[var(--color-text-secondary)] mb-10">
            Únete a la red P2P más avanzada. Renta el vehículo perfecto de anfitriones locales o genera ingresos con el tuyo.
          </p>
          
          <div className="flex flex-col sm:flex-row justify-center gap-4">
            <Button size="lg" className="animate-glow-pulse">
              Explorar Vehículos
            </Button>
            <Button size="lg" variant="secondary">
              Convertirse en Anfitrión
            </Button>
          </div>
        </div>
      </main>
    </div>
  );
};
4.3. Login.tsxTypeScript// src/pages/Login.tsx
import React, { useState } from 'react';
import { Logo } from '../components/ui/Logo';
import { Input } from '../components/ui/Input';
import { Button } from '../components/ui/Button';

export const Login = () => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    setTimeout(() => setLoading(false), 2000); // Simulate API call
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-[var(--color-surface)] px-4">
      <div className="max-w-md w-full glass-panel p-8 rounded-[var(--radius-xl)] animate-slide-up">
        
        <div className="text-center mb-8 animate-fade-in stagger-1">
          <Logo variant="icon" className="mx-auto mb-4" />
          <h2 className="text-2xl font-bold text-white">Bienvenido de vuelta</h2>
          <p className="text-[var(--color-text-secondary)] mt-2">Ingresa a tu cuenta de OmniDrive</p>
        </div>

        <form onSubmit={handleSubmit} className="space-y-6 animate-fade-in stagger-2">
          <Input 
            label="Correo electrónico" 
            type="email" 
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            required 
          />
          <Input 
            label="Contraseña" 
            type="password" 
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            required 
          />
          
          <div className="flex items-center justify-between text-sm">
            <label className="flex items-center text-[var(--color-text-secondary)]">
              <input type="checkbox" className="mr-2 rounded border-[var(--color-border)] bg-[var(--color-surface-raised)] text-[var(--color-primary)] focus:ring-[var(--color-primary)]" />
              Recordarme
            </label>
            <a href="#" className="text-[var(--color-primary)] hover:underline">¿Olvidaste tu contraseña?</a>
          </div>

          <Button type="submit" className="w-full" size="lg" isLoading={loading}>
            Iniciar Sesión
          </Button>
        </form>

        <div className="mt-6 animate-fade-in stagger-3">
          <div className="relative">
            <div className="absolute inset-0 flex items-center">
              <div className="w-full border-t border-[var(--color-border)]"></div>
            </div>
            <div className="relative flex justify-center text-sm">
              <span className="px-2 bg-[var(--color-surface-overlay)] text-[var(--color-text-muted)]">O continúa con</span>
            </div>
          </div>

          <Button variant="secondary" className="w-full mt-6" size="lg">
            <svg className="w-5 h-5 mr-2" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
              <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
              <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
              <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
              <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
            </svg>
            Google
          </Button>
        </div>
      </div>
    </div>
  );
};
4.4. VehicleCard.tsxTypeScript// src/components/VehicleCard.tsx
import React from 'react';
import { Star, MapPin } from 'lucide-react';
import { Card } from './ui/Card';
import { Badge } from './ui/Badge';

interface VehicleCardProps {
  category: string;
  brand: string;
  model: string;
  year: number;
  price: number;
  rating: number;
  trips: number;
  location: string;
  imageUrl: string;
  available?: boolean;
}

export const VehicleCard: React.FC<VehicleCardProps> = ({
  category, brand, model, year, price, rating, trips, location, imageUrl, available = true
}) => {
  return (
    <Card className="group overflow-hidden cursor-pointer flex flex-col h-full animate-fade-in">
      <div className="relative h-48 overflow-hidden">
        <img 
          src={imageUrl} 
          alt={`${brand} ${model}`} 
          className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
        />
        <div className="absolute inset-0 bg-gradient-to-t from-[#0f172a] via-transparent to-transparent opacity-60"></div>
        
        <div className="absolute top-3 left-3 flex gap-2">
          <Badge variant="cyan">{category}</Badge>
          {!available && <Badge variant="red">Reservado</Badge>}
        </div>
        
        <div className="absolute bottom-3 left-3">
          <h3 className="text-lg font-bold text-white leading-tight">{brand} {model}</h3>
          <p className="text-sm text-[var(--color-text-secondary)]">{year}</p>
        </div>
      </div>
      
      <div className="p-4 flex-1 flex flex-col justify-between">
        <div className="flex justify-between items-start mb-4">
          <div className="flex items-center text-sm text-[var(--color-text-secondary)]">
            <MapPin className="w-4 h-4 mr-1 text-[var(--color-primary)]" />
            {location}
          </div>
          <div className="flex items-center text-sm font-medium text-white">
            <Star className="w-4 h-4 mr-1 text-amber-400 fill-amber-400" />
            {rating} <span className="text-[var(--color-text-muted)] ml-1">({trips})</span>
          </div>
        </div>
        
        <div className="flex justify-between items-center pt-3 border-t border-[var(--color-border)]">
          <p className="text-xs text-[var(--color-text-muted)]">Desde</p>
          <p className="text-lg font-bold text-white">
            ${price}<span className="text-sm font-normal text-[var(--color-text-muted)]">/día</span>
          </p>
        </div>
      </div>
    </Card>
  );
};
Instrucciones de ImplementaciónRespaldo previo: Antes de aplicar estos cambios, haz un commit o un respaldo de tus archivos actuales en src/styles/ y src/components/.Sistema de Diseño: Copia el contenido del bloque de CSS y pégalo en tu archivo global de estilos (index.css o tokens.css). Esto establecerá la base oscura (slate-950) estricta que solicitaste.  Instalación de Iconos: Asegúrate de tener instalado lucide-react (npm install lucide-react). No se requieren librerías de animación externas como framer-motion.  Creación de Componentes: Crea la carpeta src/components/ui/ si no existe. Añade los archivos Logo.tsx, Button.tsx, Input.tsx, Card.tsx y Badge.tsx uno por uno.Reemplazo de Vistas: Sustituye tu actual Navbar.tsx, Home.tsx y Login.tsx con el código proporcionado. Los componentes están diseñados para ser responsivos e incluyen los efectos de backdrop-blur (glass effect).  