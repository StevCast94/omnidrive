{
  "name": "omnidrive",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx prisma db push && npx tsc",
    "start": "node backend/dist/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
