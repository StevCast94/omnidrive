{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app/web && npm ci 2>/dev/null && npm run build && rm -rf /app/backend/public && cp -r dist /app/backend/public && cd /app/backend && npm ci 2>/dev/null && npx prisma generate && npx prisma db push --skip-generate --accept-data-loss"
  },
  "deploy": {
    "startCommand": "sh entry.sh",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
