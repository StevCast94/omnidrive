{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm install --include=dev && npx prisma generate && npx tsc"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push && npx tsx prisma/seed.ts && node dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
