{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd web && npm install && npm run build && rm -rf ../backend/public && cp -r dist ../backend/public && cd ../backend && npm install && npx prisma generate && npx prisma db push --skip-generate --accept-data-loss"
  },
  "deploy": {
    "startCommand": "cd backend && sh entry.sh",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
